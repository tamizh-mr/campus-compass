
import React,{useState} from "react";import {students} from "../data/mockData";import {Card,Button,Badge,Icon} from "../components/UI";
export default function Network(){const [connected,setConnected]=useState([]);return <div className="page"><div className="pageintro"><div><span className="kicker">STUDENT NETWORK · OPT-IN</span><h1>Find people building similar things.</h1><p>Discover peers for projects, hackathons and study groups. Profiles are visible only when students opt in.</p></div></div><div className="notice"><Icon name="ShieldCheck"/><span>Your networking profile visibility is controlled from Profile → Privacy.</span></div><div className="networkgrid">{students.map(s=><Card className="student" key={s.id}><div className="studenttop"><span className="studentavatar">{s.name.split(" ").map(x=>x[0]).join("").slice(0,2)}</span><div><h3>{s.name}</h3><p>{s.dept} · {s.year}</p></div></div><p className="college"><Icon name="GraduationCap"/>{s.college}</p><small>SKILLS</small><div className="tags">{s.skills.map(x=><span key={x}>{x}</span>)}</div><div className="looking"><Icon name="Search"/><span><small>LOOKING FOR</small><b>{s.looking}</b></span></div><Button variant={connected.includes(s.id)?"secondary":"primary"} onClick={()=>setConnected(x=>x.includes(s.id)?x.filter(i=>i!==s.id):[...x,s.id])}>{connected.includes(s.id)?"Connected":"Connect"}</Button></Card>)}</div></div>}
