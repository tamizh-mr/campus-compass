
import React,{useState} from "react";import {events} from "../data/mockData";import {Card,Badge,Button,Icon} from "../components/UI";
export default function Events(){const [mode,setMode]=useState("All");return <div className="page"><div className="pageintro"><div><span className="kicker">EVENTS</span><h1>Something worth showing up for.</h1><p>Discover workshops, technical events, meetups and campus experiences.</p></div></div><div className="pilltabs">{["All","Online","Offline"].map(x=><button className={mode===x?"active":""} onClick={()=>setMode(x)} key={x}>{x}</button>)}</div><div className="eventgrid">{events.filter(e=>mode==="All"||e.mode===mode).map(e=><Card className="event" key={e.id}><div className="eventdate"><b>{e.date.split(" ")[0]}</b><span>{e.date.split(" ")[1]}</span></div><div className="grow"><div className="tags"><Badge>{e.type}</Badge><Badge>{e.mode}</Badge></div><h2>{e.title}</h2><p>{e.organizer}</p><span className="eventloc"><Icon name="MapPin"/>{e.location}</span></div><Button variant="secondary">View event</Button></Card>)}</div></div>}
