
import React,{useState} from "react";import {subjects} from "../data/mockData";import {Card,Icon,Badge} from "../components/UI";
export default function Academics(){const [subject,setSubject]=useState(subjects[0]);return <div className="page"><div className="pageintro"><div><span className="kicker">ACADEMICS · B.E. ECE</span><h1>Semester 3 workspace.</h1><p>Keep the material you actually need within reach.</p></div><Badge>4 subjects</Badge></div><div className="academicgrid"><div className="subjectlist">{subjects.map(s=><button onClick={()=>setSubject(s)} className={subject.name===s.name?"active":""} key={s.name}><span className="roundicon"><Icon name="BookOpen"/></span><div><b>{s.name}</b><small>{s.code}</small></div><Icon name="ChevronRight"/></button>)}</div><Card className="resourcepanel"><span className="kicker">{subject.code}</span><h2>{subject.name}</h2><p>Prototype resources for this subject.</p><div className="resources">{subject.resources.map((r,i)=><button key={r}><span className="resourceicon"><Icon name={i===0?"FileText":i===1?"NotebookTabs":i===2?"Files":"Link"}/></span><div><b>{r}</b><small>Open demo resource</small></div><Icon name="ArrowUpRight"/></button>)}</div></Card></div></div>}
