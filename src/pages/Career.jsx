
import React,{useState} from "react";import {opportunities} from "../data/mockData";import {Card,Badge,Button,Icon} from "../components/UI";
export default function Career(){const [type,setType]=useState("All");const types=["All","Internship","Hackathon","Certification","Jobs","Courses","Competitions"];return <div className="page"><div className="pageintro"><div><span className="kicker">CAREER DEVELOPMENT</span><h1>Build toward what comes next.</h1><p>Explore internships, hackathons, certifications and preparation resources.</p></div></div><div className="pilltabs">{types.map(t=><button className={type===t?"active":""} onClick={()=>setType(t)} key={t}>{t}</button>)}</div><div className="opplist">{opportunities.filter(o=>type==="All"||o.type===type).map(o=><Card className="oppcard" key={o.id}><div className="oppicon"><Icon name={o.type==="Hackathon"?"Trophy":o.type==="Certification"?"BadgeCheck":"BriefcaseBusiness"}/></div><div className="grow"><div className="tags"><Badge>{o.type}</Badge><Badge>{o.match}% match</Badge></div><h2>{o.title}</h2><p>{o.org}</p><div className="meta"><span><Icon name="MapPin"/>{o.location}</span><span><Icon name="Clock3"/>{o.days} days left</span></div><div className="tags">{o.skills.map(x=><span key={x}>{x}</span>)}</div></div><div className="verticalactions"><Button>View details</Button><Button variant="secondary"><Icon name="Bookmark" size={16}/> Save</Button></div></Card>)}</div></div>}
