
import React,{useState} from "react";import {Button,Icon} from "../components/UI";
export default function Auth({mode,go}){
 const [name,setName]=useState(""); const submit=e=>{e.preventDefault();localStorage.setItem("cc_user",name||"Arjun");go(mode==="register"?"onboarding":"dashboard")};
 return <div className="authpage"><button className="brand authbrand" onClick={()=>go("landing")}><span><Icon name="Compass"/></span><div>Campus<b>Compass</b></div></button><div className="authbox"><div><span className="authicon"><Icon name={mode==="login"?"LogIn":"UserPlus"}/></span><h1>{mode==="login"?"Welcome back":"Create your compass"}</h1><p>{mode==="login"?"Continue your student journey.":"A few details, then Campus Compass becomes yours."}</p></div><form onSubmit={submit}>{mode==="register"&&<label>Name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label>}<label>Email<input required type="email" placeholder="you@example.com"/></label><label>Password<input required type="password" placeholder="••••••••"/></label><Button>{mode==="login"?"Log in":"Create account"} <Icon name="ArrowRight" size={17}/></Button></form><p className="authswitch">{mode==="login"?"New here? ":"Already have an account? "}<button onClick={()=>go(mode==="login"?"register":"login")}>{mode==="login"?"Create account":"Log in"}</button></p></div></div>
}
