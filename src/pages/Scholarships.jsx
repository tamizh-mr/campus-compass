
import React,{useState} from "react";import {scholarships} from "../data/mockData";import {Card,Badge,Button,Icon} from "../components/UI";
export default function Scholarships(){const [open,setOpen]=useState(null);return <div className="page"><div className="pageintro"><div><span className="kicker">SCHOLARSHIPS</span><h1>Funding that fits your profile.</h1><p>Demo eligibility explanations based on the current mock student profile.</p></div><Button variant="secondary"><Icon name="SlidersHorizontal" size={16}/> Filters</Button></div><div className="notice"><Icon name="Info"/><span>These are fictional/demo scholarship records for the frontend prototype — not live application information.</span></div><div className="scholargrid">{scholarships.map(s=><Card className="scholar" key={s.id}><div className="rowtop"><span className="awardicon"><Icon name="Award"/></span><Badge>{s.match}% profile match</Badge></div><h2>{s.name}</h2><p>{s.provider}</p><div className="benefit"><small>BENEFIT</small><b>{s.benefit}</b></div><div className="schmeta"><span><Icon name="CalendarDays"/>{s.deadline}</span><span><Icon name="Clock3"/>{s.days} days remaining</span></div><button className="why" onClick={()=>setOpen(open===s.id?null:s.id)}>Why does this match me? <Icon name={open===s.id?"ChevronUp":"ChevronDown"} size={16}/></button>{open===s.id&&<div className="whylist">{s.why.map(x=><span key={x}><Icon name="Check" size={15}/>{x}</span>)}</div>}<div className="cardactions"><Button>View details</Button><Button variant="secondary">Save</Button></div></Card>)}</div></div>}
